{
  "name": "mylang",
  "publish": false,
  "version": "0.0.1",
  "scripts": {
    "test": "cd precompile && mocha --reporter spec --bail --ui bdd --colors --compilers coffee:coffee-script -w test/unit/*.coffee",
    "debug": "killall -9 node; npm run-script prestart && slnode debug -s static/main.js precompile/test/fixtures/ /tmp/",
    "prestart": "coffee --bare -o static/ precompile/"
  },
  "devDependencies": {
    "coffee-script": "~1.6.3",
    "chai": "~1.7.2",
    "mocha": "~1.12.0"
  },
  "dependencies": {
    "glob": "~3.2.6",
    "async2": "~0.1.7"
  }
}
